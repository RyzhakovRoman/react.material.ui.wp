// import React from 'react';

// export default () => console.log('App', React);

export default class App extends React.Component {
    render() {
        const name = 'App';

        return (
            <h1>{name}</h1>
        )
    }
}